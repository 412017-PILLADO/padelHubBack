package org.example.padelback.modules.pagos.domain.model;

/** Preferencia de Checkout Pro creada para cobrar una seña. initPoint = URL de pago. */
public record PreferenciaSena(String preferenceId, String initPoint) {}
